# Secrets — never commit router API passwords, wallet keys, or env files
.env
*.local
config.local.*
*.key
*.pem
wallet.json

# Go
morctl/morctl
morctl/*.exe
morctl/bin/

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/
